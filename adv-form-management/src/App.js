import React, { useState } from 'react';
import logo from './logo.svg';
import './App.css';
import Form from './components/Form';

function App() {
  return (
    <div className="App">
      <Form
      // values={formValues}
      // disabled={disabled}
      // errors={formErrors}
      // change={inputChange}
      />
    </div>
  );
}

export default App;
